# OpenAI-compatible endpoint used only by /画本. Leave empty to use fallback annotation.
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini

# ASR runtime. CPU + INT8 is the default.
ASR_MODEL=small
ASR_DEVICE=cpu
ASR_COMPUTE_TYPE=int8
